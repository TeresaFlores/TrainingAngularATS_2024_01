import { CapitalizarPalabraDirective } from './capitalizar-palabra.directive';

describe('CapitalizarPalabraDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizarPalabraDirective();
    expect(directive).toBeTruthy();
  });
});
