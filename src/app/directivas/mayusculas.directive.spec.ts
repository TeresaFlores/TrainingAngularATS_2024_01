import { MayusculasDirective } from './mayusculas.directive';

describe('MayusculasDirective', () => {
  it('should create an instance', () => {
    const directive = new MayusculasDirective();
    expect(directive).toBeTruthy();
  });
});
